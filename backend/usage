->Install NodeJS

->While in the directory ./backend, run npm install to get the dependencies.

->Download and put the yolo weights in ./backend (download from google drive)

->Run node app.js to start the server on localhost port 3000
